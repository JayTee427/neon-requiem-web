import fs from 'node:fs';const root='public/content/';const bin=fs.readFileSync(root+'geometry.bin');const a=JSON.parse(fs.readFileSync(root+'actors/SableVoss.json'));for(const c of a.clips){let changed=[];for(const t of c.tracks){const d=t.data;const f=new Float32Array(bin.buffer,bin.byteOffset+d.offset,d.count);let max=0;for(let k=7;k<f.length;k++)max=Math.max(max,Math.abs(f[k]-f[k%7]));if(max>.001)changed.push(a.nodes[t.node].name);}console.log(c.name,changed.length,changed.slice(0,12));}
