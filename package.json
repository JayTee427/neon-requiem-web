{
  "name": "neon-requiem-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "package:windows": "npm run build && node tools/package.mjs",
    "serve:release": "node tools/serve.mjs --root release/NeonRequiemWeb"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "vite": "8.2.2",
    "@playwright/test": "1.58.2"
  }
}
